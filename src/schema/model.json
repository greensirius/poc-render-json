{
  "type": "object",
  "description": "A model describes a single record in a data set (collection). This includes specifying the fields that record has and also zero or more \"computed values\" which are derived from data fields.",
  "properties": {
    "name": { "type": "string" },
    "schema": {
      "$ref": "http://json-schema.org/draft-07/schema#"
    },
    "computed_values": {
      "type": "object",
      "properties": {},
      "additionalProperties": { "type": "string" }
    }
  },
  "required": ["name", "schema"],
  "additionalProperties": false
}
